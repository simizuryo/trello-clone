# スコープ外・拡張ロードマップ

## 10. スコープ外(v1では対応しない・将来の拡張候補)
- 複数ボードの管理
- カード詳細(説明文、ラベル、担当者、添付ファイル、チェックリスト)
- ユーザー認証・複数人でのリアルタイム共有
- カードの削除(検索(サーバー)画面側。マイボード画面では実装済み。別Issueで対応予定)
- 通知機能

v4ロードマップの前倒し実施により、当初ここに挙げていた「サーバーサイド保存(DB連携)」「検索・フィルター機能」は実装済みのため、スコープ外から除外した(詳細は11節を参照)。

## 11. 拡張ロードマップ(参考)
- v2: カード詳細(説明など残りの項目)を追加
- v3: 複数ボード対応
- v4: バックエンド連携(Java + Spring Boot製REST API + PostgreSQL)。IndexedDBでの永続化に加えて、バックエンドAPI経由の永続化(検索(サーバー)画面)を追加する。複数人での共有・認証などはさらに先の検討課題とする
  - 2026-08-02時点: `docker-compose.yml`(PostgreSQL)と`backend/`(Spring Boot雛形・DB接続確認)を追加
  - 2026-08-06時点: List/CardのREAD API(検索機能付き)を実装
  - 2026-08-07〜08-09時点: フロントエンドに「検索(サーバー)」画面を追加し、検索結果表示・カード/リストの新規作成をAPI連携で実装
  - 2026-08-10時点: 検索(サーバー)画面にカードのUpdate操作(タイトル・優先度・期限のインライン編集、ドラッグ&ドロップによる並び替え・リスト間移動)を実装。カードのDeleteのみ未実装(別Issueで対応予定)
